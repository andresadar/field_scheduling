/// Clase abstracta que centraliza la definición de
/// las rutas utilizadas en la navegación de la aplicación.
abstract class MyRoutes {
  /// Ruta para la pantalla principal de reservas.
  static const String home = "home";

  /// Ruta para la pantalla de guardar una nueva reserva.
  static const String saveBooking = "/save_booking";
}
